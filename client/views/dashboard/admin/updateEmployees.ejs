<!DOCTYPE html>
<html lang="en" class="h-full scroll-smooth">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link rel="icon" href="/images/logo-blanc.png" type="image/x-icon">
    <link type="text/css" rel="stylesheet" href="/css/output.css" />
    <link rel="stylesheet" href="/css/style.css">
    <script src="https://kit.fontawesome.com/1126a5f03b.js" crossorigin="anonymous"></script>
</head>
<body class=" min-h-screen relative ">
<!-- Nav Bar -->
<header class="relative z-10 ">
    <%- include('../../components/dashboard/nav-bar-dashboard') %>
</header>

<!-- Cinema and Date Filters -->
<main class="flex text-blueOne w-full flex-col items-center gap-y-7 justify-center md:w-[85vw]  ml-auto md:pt-40 " id="main-container">

    <section class="container w-full  md:w-[85vw]  ml-auto flex flex-col mt-10 ">

        <form action="/api/v1/users/<%= users.user_id %>?_method=PUT" method="post" class="flex flex-col relative items-center justify-center bg-whiteOne w-[90%] md:w-[55%] mx-auto rounded-sm px-6 py-5 space-y-5 md:py-10 md:space-y-8" id="update-form">

            <h1 class="text-blueOne font-arvo text-3xl tracking-wide font-bold text-center">MODIFIER EMPLOYÉS</h1>
          
            <ul class="flex-col items-start justify-center space-y-1 w-fit mx-auto hidden" id="error-list">
            </ul>
          
            <div class="w-full h-16 bg-white flex items-center justify-center pl-4">
              <input type="text" class="outline-none flex-1 w-full h-full flex items-center placeholder:text-lg placeholder:font-arvo placeholder:font-bold" placeholder="<%= users.first_name %>" name="first_name" value="<%= users.first_name %>">
            </div>
          
            <div class="w-full h-16 bg-white flex items-center justify-center pl-4">
              <input type="text" class="outline-none flex-1 w-full h-full flex items-center placeholder:text-lg placeholder:font-arvo placeholder:font-bold" placeholder="<%= users.last_name %>" name="last_name" value="<%= users.last_name %>">
            </div>
          
            <div class="w-full h-16 bg-white flex items-center justify-center pl-4">
              <input type="text" class="outline-none flex-1 w-full h-full flex items-center placeholder:text-lg placeholder:font-arvo placeholder:font-bold" placeholder="<%= users.username %>" name="username" value="<%= users.username %>">
            </div>
          
            <div class="w-full h-16 bg-white flex items-center justify-center pl-4">
              <input type="email" class="outline-none flex-1 w-full h-full flex items-center placeholder:text-lg placeholder:font-arvo placeholder:font-bold" placeholder="<%= users.email %>" name="email" value="<%= users.email %>">
            </div>
          
            <div class="w-full h-16 bg-white flex items-center justify-center pl-4">
              <input type="password" class="outline-none flex-1 w-full h-full flex items-center placeholder:text-lg placeholder:font-arvo placeholder:font-bold" placeholder="MOT DE PASSE" name="password" value="">
            </div>
          
            <div class="w-full h-16 bg-white flex items-center justify-center pl-4">
              <input type="password" class="outline-none flex-1 w-full h-full flex items-center placeholder:text-lg placeholder:font-arvo placeholder:font-bold" placeholder="CONFIRMER MOT DE PASSE" name="confirmPassword" value="">
            </div>
          
            <button id="validate-button" class="mx-auto bg-redOne font-arvo text-white font-bold text-lg px-10 py-2 mt-2 flex hover:scale-95 duration-200 ease-in-out items-center justifify-center">Valider</button>
          
            <!-- hidden alert pop up -->
            <div class="absolute z-50 animate-open-menu hidden flex-col items-center justify-around bg-blueOne/75 w-[50vw] h-[100vh] rounded-lg px-10 py-48" id="alert">
              <p class="text-center font-arvo text-white font-bold text-3xl">VOULEZ-VOUS VRAIMENT MODIFIER L'EMPLOYÉ "<%= users.first_name %>" ?</p>
              <div class="flex items-center justify-center space-x-10">
                <button type="submit" id="submit-form" class="bg-green-500 w-36 h-14 text-white font-arvo font-bold flex items-center justify-center hover:scale-95 duration-200 hover:bg-green-400">OUI</button>
                <button id="close-alert" class="bg-redOne w-36 h-14 text-white font-arvo font-bold flex items-center justify-center hover:scale-95 duration-200 hover:bg-red-500">NON</button>
              </div>
            </div>
        </form>

    </section>

</main>

<!-- Side Bar Menu -->
<aside class="fixed z-20 top-0 left-0 h-screen">
    <%- include('../../components/dashboard/side-menu') %>
</aside>

<!-- Footer -->
<footer class="pt-[400px]">
    <%- include('../../components/footer') %>
</footer>

<script src="/js/selectUpdate.js"></script>

</body>
</html>